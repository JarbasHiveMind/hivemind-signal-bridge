name: Code Coverage

on:
  pull_request:
    branches: [feat/signal-bridge, dev]
  workflow_dispatch:

jobs:
  coverage:
    uses: OpenVoiceOS/gh-automations/.github/workflows/coverage.yml@dev
    with:
      python_version: '3.11'
      coverage_source: 'hivemind_signal_bridge'
      test_path: 'tests/'
      test_extras: 'test'
